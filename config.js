/**
 * Configuración de la app empaquetada.
 * Un solo lugar para cambiar la dirección del servidor cuando se mude
 * de esta prueba local al VPS/nube definitivo — no requiere tocar nada
 * más, y el usuario final nunca necesita escribirla.
 */
'use strict';

module.exports = {
  SERVIDOR: 'http://192.168.3.165:8000',
};
